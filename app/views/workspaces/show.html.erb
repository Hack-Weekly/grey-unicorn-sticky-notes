<div class="container mt-4">
  <p class="text-success"><%= notice %></p>

  <div class="card">
    <div class="card-body">
      <%= render @workspace %>
    </div>
  </div>

  <div class="mt-4">
    <%= link_to "Add Whiteboard", new_workspace_whiteboard_path(@workspace), class: 'btn btn-sm btn-outline-success' %>
    <%= link_to "Edit this workspace", edit_workspace_path(@workspace), class: 'btn btn-sm btn-outline-primary' %>
    <%= link_to "Back to sticky notes", root_path, class: 'btn btn-sm btn-outline-secondary' %>

    <%= button_to "Delete", @workspace, method: :delete, class: 'btn btn-sm btn-outline-danger', data: { confirm: 'Are you sure?' } %>
  </div>
</div>
